# Muse Vite Template

Minimal Vue+Vite starter used by Muse's onboarding "Create a new
repository" flow (scaffolded via GitHub's "Generate repository from
template" API). Add your real design-system components under `src/`.
